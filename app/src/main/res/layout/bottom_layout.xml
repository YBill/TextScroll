<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:id="@+id/outer_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#33000000"
        android:clickable="true"
        android:orientation="vertical"
        android:paddingLeft="10dp"
        android:paddingRight="10dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="10dp">

            <TextView
                android:id="@+id/act_image_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/act_img_index"
                android:textColor="#ffffff"
                android:textSize="17sp"
                tools:text="title" />

            <TextView
                android:id="@+id/act_img_index"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/act_img_size"
                android:textColor="#ffffff"
                android:textSize="19sp"
                tools:text="1" />

            <TextView
                android:id="@+id/act_img_size"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignBottom="@id/act_img_index"
                android:layout_alignParentRight="true"
                android:layout_marginBottom="2dp"
                android:textColor="#ffffff"
                android:textSize="12sp"
                tools:text="/5" />
        </RelativeLayout>

        <TextView
            android:id="@+id/bot_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="9dp"
            android:lineSpacingExtra="3dp"
            android:maxHeight="@dimen/content_max_height"
            android:minHeight="@dimen/content_min_height"
            android:scrollbars="vertical"
            android:textColor="#ffffff"
            android:textSize="13sp"
            tools:text="就是因为它的因为的它的不年" />
    </LinearLayout>
</merge>
